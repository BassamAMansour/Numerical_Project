from equation_solvers.EquationSolver import EquationSolver
from equation_solvers.Root import Root


class BiergeVieta(EquationSolver):


    def getCoeffecients(equation):
        coeff = []
        return coeff

    # Add suitable args
    def __init__(self,  equation, initialValue):
        super().__init__(equation)
       #upload bierge vieta ya akram

       # for testing
       # for i in range (0,6) :
       #     root = Root(0,0)
       #     root.precision = 5 + i
       #     root.root = 2 + i
       #     self.add_root(root)

